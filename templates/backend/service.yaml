apiVersion: v1
kind: Service
metadata:
  name: lms-be-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
  selector:
    app: lms-be
